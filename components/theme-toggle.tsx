'use client';
import {useEffect,useState} from 'react';
import {Moon,Sun} from 'lucide-react';
import {Switch} from '@/components/ui/switch';
export default function ThemeToggle(){const [dark,setDark]=useState(false);useEffect(()=>{const media=window.matchMedia('(prefers-color-scheme: dark)');let saved:string|null=null;try{saved=localStorage.getItem('omar-theme')}catch{}const value=saved?saved==='dark':media.matches;setDark(value);document.documentElement.classList.toggle('dark',value);function change(e:MediaQueryListEvent){let pref=null;try{pref=localStorage.getItem('omar-theme')}catch{}if(!pref){setDark(e.matches);document.documentElement.classList.toggle('dark',e.matches)}}media.addEventListener('change',change);return()=>media.removeEventListener('change',change)},[]);return <div className="theme-control"><span aria-hidden="true"><Sun size={16} strokeWidth={2} fill="currentColor"/></span><Switch aria-label="Dark mode" checked={dark} onCheckedChange={value=>{setDark(value);document.documentElement.classList.toggle('dark',value);try{localStorage.setItem('omar-theme',value?'dark':'light')}catch{}}}/><span aria-hidden="true"><Moon size={16} fill="currentColor" strokeWidth={0}/></span></div>}

